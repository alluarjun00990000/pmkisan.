<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login</title>
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
  <style>
    /* Reset & Global Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #1c1f26, #2e323a);
      color: #fff;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    /* Animated 3D Header */
    header {
      text-align: center;
      margin-bottom: 30px;
      color: #00ffcc;
      font-size: 2.5rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
      text-shadow: 2px 4px 10px rgba(0, 255, 255, 0.8), 0px 8px 20px rgba(0, 128, 255, 0.7);
      animation: slideIn 2.5s ease-in-out infinite alternate;
      perspective: 1000px;
    }
    @keyframes slideIn {
      from { transform: translateY(-10px) rotateX(10deg); opacity: 0.8; }
      to { transform: translateY(0px) rotateX(0deg); opacity: 1; }
    }
    /* Glassmorphism Container */
    .login-wrapper {
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 12px;
      backdrop-filter: blur(8px);
      -webkit-backdrop-filter: blur(8px);
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
      padding: 40px 30px;
      max-width: 400px;
      width: 100%;
      text-align: center;
    }
    h2 {
      font-size: 2rem;
      margin-bottom: 20px;
      color: #00b4d8;
    }
    /* Form Styling */
    .login-form {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    .login-form input {
      padding: 15px;
      font-size: 1rem;
      border: none;
      border-radius: 8px;
      background: rgba(255, 255, 255, 0.1);
      color: #fff;
      outline: none;
      transition: background 0.3s ease, box-shadow 0.3s ease;
    }
    .login-form input::placeholder {
      color: #ccc;
    }
    .login-form input:focus {
      background: rgba(255, 255, 255, 0.15);
      box-shadow: 0 0 8px rgba(0, 180, 216, 0.7);
    }
    .login-form button {
      padding: 15px;
      font-size: 1rem;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      background: linear-gradient(90deg, #00b4d8, #0077b6);
      color: #fff;
      cursor: pointer;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .login-form button:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(0, 180, 216, 0.5);
    }
    .error-message {
      color: #ff6b6b;
      font-size: 0.9rem;
      margin-bottom: 10px;
    }
    .footer {
      margin-top: 20px;
      font-size: 0.9rem;
      color: #aaa;
    }
  </style>
</head>
<body>
  <!-- 3D Animated Header -->
  <header>Shadow Hacker</header>
  
  <!-- Glassmorphism Login Container -->
  <div class="login-wrapper">
    <h2>Login</h2>
    <% if (error) { %>
      <p class="error-message"><%= error %></p>
    <% } %>
    <form class="login-form" action="/login" method="POST">
      <input type="text" name="username" placeholder="Username" required>
      <input type="password" name="password" placeholder="Password" required>
      <button type="submit">Login</button>
    </form>
    <div class="footer">
      &copy; 2025 Shadow Hacker Pvt. Ltd.
    </div>
  </div>
</body>
</html>
